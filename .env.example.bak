# Backend Environment Variables

# Application Configuration
# APP_NAME: Custom name for the application (default: Bob)
# APP_NAME=Bob

# JEFF_MODE: Special mode that renames app to "Jeff" and restricts to Kiro agent only
# When enabled: app name becomes "Jeff", only Kiro agent is available
# JEFF_MODE=false

# USE_GITHUB_AUTH: Enable/disable GitHub authentication
# Enabled by default for Docker/VPS deployments
# Set to false for local development if you don't need auth
USE_GITHUB_AUTH=true

# GitHub OAuth Configuration
# To set up GitHub OAuth:
# 1. Go to GitHub Settings > Developer settings > OAuth Apps
# 2. Click "New OAuth App"
# 3. Fill in the following:
#    - Application name: Bob Development Tool
#    - Homepage URL: https://claude.gmac.io
#    - Authorization callback URL: https://claude.gmac.io/api/auth/github/callback
# 4. Copy the Client ID and Client Secret below

GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here
GITHUB_CALLBACK_URL=https://claude.gmac.io/api/auth/github/callback

# Production URL (required for production deployments)
FRONTEND_URL=https://claude.gmac.io

# User allowlist (comma-separated GitHub usernames, leave empty to allow all authenticated users)
# GITHUB_USER_ALLOWLIST=user1,user2,user3

# Session secret for Express sessions (CHANGE THIS in production!)
# Generate with: openssl rand -base64 32
SESSION_SECRET=change-this-secret-in-production-use-a-long-random-string

# Node environment (production or development)
NODE_ENV=production

# Port configuration (optional, defaults shown)
# In Docker, use 3001; otherwise 43829 for local dev
PORT=3001

# ============================================
# Docker-specific Configuration
# ============================================

# Host directory for repository clones (mounted in container)
# HOST_REPOS_DIR=/home/bob/repos

# ============================================
# Agent Configuration
# ============================================
# Note: When running in Docker, AI agent CLIs (claude, codex, gemini, etc.)
# must be installed in the container. See Dockerfile for details.
