services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: gmacko
      POSTGRES_PASSWORD: gmacko
      POSTGRES_DB: gmacko
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
