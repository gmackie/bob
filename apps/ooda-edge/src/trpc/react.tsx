/**
 * Placeholder tRPC React provider — passes children through.
 * Task 8 will fill in the real tRPC wiring.
 */
"use client";

export function TRPCReactProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
