# Production Deploy

Bob launches on Cloudflare Workers through `apps/blder/wrangler.jsonc` and
`pnpm --filter @bob/blder deploy`.

The previous Node/VPS launch files for `claude.gmac.io` were removed so this
directory does not advertise a second production target.
