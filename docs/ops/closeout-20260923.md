# September 2026 workspace closeout

Reviewed against `master` at `7863b955`. Recovery branches under
`archive/cleanup-20260922/` are historical snapshots, not pending integration.

| Saved work | Disposition |
| --- | --- |
| `deep-report-execution` | Already implemented: `workItemsDispatch` calls `createExecutionBranch`; helper and regression test are present on master. |
| `operation-reports` | Already merged; `411d5dd0` is an ancestor of master. |
| `mega-pr` | Fully covered: a trial merge produces exactly master's tree (`ccd3944a72140cbdbba9ddc35cda34f41f1522e2`). |
| `deep-traces` | Integrated by this closeout: preserved CI artifacts, independent publication, bounded retries and missing-artifact failure reporting. |
| `seed` | OpenAI-compatible routes, implementation and tests already present. Keep master's corrected route import. Old SDK migration patch, typecheck logs and historical report variants remain recovery material, not changes to replay. |
| `otel-publishing` | Historical September 15 production acceptance receipt, retained at `c73382a7`. It describes that deployment, not current production health. |
| Standalone job-timeouts clone | Timeouts already present in CI and Preflight workflows. |
| Standalone SigNoz logs/traces clones | Destinations already configured in all three Worker configurations. |

## Historical acceptance receipt

The original September 15 receipt remains available in
[the archived telemetry document](https://github.com/gmackie/bob/blob/c73382a7df48af3ba3a3f143b49821a205785a1f/docs/plans/2026-09-15-otel-publishing.html).
It records exact trace canaries, deployment identity and coverage limits.
This closeout does not claim a fresh production deployment or telemetry check.

## Old stashes

The six stashes were reviewed and remain recoverable with their original
metadata and binary patches in `~/bob-cleanup-20260922`.

- SDLC plan annotations are historical status claims. They are retained as
  evidence rather than applied to overwrite later plan revisions.
- Android preflight output and iOS regeneration changes are stale generated
  state and must not replace the current production mobile project.
- The anti-slop stash's dependencies already exist. Its proposal to make a
  repository-wide oxlint pass mandatory is retired from this cleanup; enabling
  a new global lint gate requires its own baseline and rollout.
- The repair prompt's prohibition on empty commits is already implemented.
- The pipeline-visibility stash contains no tracked changes.
- The OODA runner command builder and `cursor-agent` executable correction are
  already implemented; its remaining generated mobile/lockfile changes are stale.

## Verification

Node 24: 27 CI reporter, deploy-gate and install-scope regression tests pass.
The workflow parses successfully and retains both existing deployment jobs.
Application source and deployment gates are unchanged by this closeout.
