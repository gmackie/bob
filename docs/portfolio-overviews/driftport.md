# Portfolio overview: Driftport

Pulse commands `agent context` / `growth review` expect `--startup <uuid>` (slug `driftport` is rejected). Data uses ID `8fa19232-2b76-44ce-88fd-34aa8ef27c44` from `pulse status --json`.

## Lifecycle stage

**Prototype** lifecycle; portfolio role **backburner** (gmacko-owned). Focus: Van/RV IoT gateway (ESP32-S3), CAN/Modbus/NMEA2000 bridging, network intelligence dashboard; repo `vancontrol`, formerly VanControl. Operator notes: repo maintained, not actively invested.

## Health grade

**A**, **thriving**, ops score **85**. Only **operational (85)** is measured; financial, product, reliability, infrastructure, compliance, and growth dimensions show **no data** yet.

## Key metrics

- Ops: **85 / A**, status headline: all systems green.  
- Growth: **no objectives**, **no campaigns**, **no recent actions** in context pack.  
- Integrations: **none** listed on the startup record.

## Top bottleneck

**Growth review**: no ranked bottlenecks and no urgent headline constraint. Practically, the limiter is **portfolio priority** (backburner, limited investment) plus **missing product and GTM instrumentation**—without connectors, growth and product scores stay dark.

## Recommended next action

If Driftport leaves backburner: wire **PostHog, Sentry, Stripe, ForgeGraph, CRM/Twenty, entity/compliance** so ops reflects usage and revenue (Pulse portfolio top action). Until then, run `pulse campaigns list` / `pulse actions list` on this ID only for deliberate experiments or maintenance.
