# Portfolio overview: controlsfoundry

**Generated from BizPulse (`pulse`), slug `controlsfoundry`.** `pulse agent context --json` and `pulse growth review --json` require `--startup <uuid>` (`8704b9b0-8720-4af2-89d4-fd62cd046d7b`); the slug alone is rejected.

**Lifecycle stage:** Product lifecycle **prototype**; portfolio role **backburner**. **ControlsFoundry** (formerly **EdgeOps**), repo **edgeops**. Naming resolved; **Gmacko LLC**–owned.

**Health grade:** **A**, status **thriving**, composite **ops score 85**. Only **operational** contributes (**85**); financial, product, reliability, infrastructure, compliance, and growth dimensions **do not score** yet.

**Key metrics:** Pulse agent context shows **no objectives**, empty recent actions/playbooks/campaigns, and **no integration rows** — there are **no KPI or funnel signals** in the pack beyond ops composite.

**Top bottleneck:** `pulse growth review` reports **no urgent bottleneck**. In practice the constraint is **sparse measurable pipeline**: backburner prioritization plus missing connectors/objectives leaves growth reviews without evidence groups to rank.

**Recommended next action:** Connect **Stripe, QuickBooks, PostHog, Sentry, ForgeGraph, entity/compliance, and CRM/Growth** (per Pulse ops checklist), then add **at least one concrete growth objective** so weekly reviews can propose experiments against live telemetry.
