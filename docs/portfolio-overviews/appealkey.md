# Portfolio overview: appealkey

**Generated from BizPulse (`pulse`), slug `appealkey`.** JSON subcommands require `--startup <uuid>` (`ae53e174-68bb-4729-9320-6e9c85467376` from `pulse status --json`); the slug alone is rejected.

**Lifecycle stage:** Product lifecycle **launched**; portfolio role **incubating**. Domain **appealkey-default.forgegraf.com**, repo **appealkey**. Operator notes describe **AppealKey property tax appeal automation** (ForgeGraph import).

**Health grade:** **B**, status **healthy**, composite **ops score 73**. **Operational** scores **87**; **infrastructure** scores **55**. Financial, product, reliability, compliance, and growth dimensions are **not yet scoring** in the scorecard.

**Key metrics:** KPI snapshot shows **MRR 0**, **0 customers**, **0 DAU**. Linear, ForgeGraph, Twenty, Stripe, PostHog, and App Store Connect show **connected**; **Sentry** is **`auth_failed`**. Portfolio urgency flags **stale ForgeGraph and Stripe syncs** relative to last successful pulls.

**Top bottleneck:** `pulse growth review` reports **no urgent bottleneck** and an empty bottleneck list. In practice, visibility is capped by **failed error tracking**, **stale connector data**, **unset headline revenue/usage KPIs**, and **no active growth objectives** in agent context.

**Recommended next action:** **Fix Sentry authentication**, **refresh ForgeGraph and Stripe syncs**, then connect remaining scorecard inputs (**QuickBooks, entity/compliance, CRM/Growth**) and add a **concrete growth objective** so acquisition and revenue evidence can surface in reviews.
