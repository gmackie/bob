# Portfolio overview: BizPulse

Via **pulse**: `pulse agent context` / `pulse growth review` (**use startup UUID**, not slug; BizPulse **`bd47227f-c44a-4b49-99a6-a276a1fdaf5f`**), plus `pulse startup show` and **`pulse status --json`**.

**Lifecycle:** **Prototype**, portfolio **incubating**, **Gmacko LLC**‑owned former **Pulse Manager** line (`bizpulse.cc`, **`pulse`** repo, **`com.gmacko.bizpulse`**).

**Health:** **A / thriving**, **ops score 83** driven by **operational** inputs; financial, product, reliability, growth, and compliance are mostly **unset or unavailable**.

**Key metrics:** no growth objectives; **no startup alerts**. **PostHog** and **App Store Connect** connected; PostHog’s last recorded sync (**2026-05-07**) is stale vs now. **Sentry**: **`auth_failed`**. **`pulse status`** still queues BizPulse under **daily / stabilize** (production/revenue triage alongside peers).

**Top bottleneck:** **Telemetry coverage**, despite **growth review** finding **no urgent bottleneck**. Broken error ingest and quiet scorecard pillars hide product/revenue risk until incidents land.

**Next action:** **Fix Sentry auth**, force a **PostHog resync**, then **connect Stripe/QuickBooks/ForgGraph/CRM** (per Pulse prompts) before prioritizing speculative growth motions.
