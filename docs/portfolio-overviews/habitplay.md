# Portfolio overview: habitplay

**Generated from BizPulse (`pulse`), slug `habitplay`.** `pulse agent context --json` and `pulse growth review --json` require `--startup <uuid>` (`eae11898-8405-4084-b92b-12c7e748e471` from `pulse status --json`); the slug alone fails UUID validation.

**Lifecycle stage:** Product lifecycle **prototype**; portfolio role **incubating**. **HabitPlay** (iOS), bundle `com.gmacko.habitplay`, domain **habitplay.io**, repo **habit**. Former names **HabbitPlay.io**, **Habit App**. Naming **resolved**; **Gmacko LLC**–owned.

**Health grade:** **B**, status **healthy**, composite **ops score 77**. Only **operational** contributes (**77**); financial, product, reliability, infrastructure, compliance, and growth dimensions **do not score** yet. Status headline cites **stale connector sync**.

**Key metrics:** Growth review carried **no KPI evidence** (empty acquisition through revenue groups). **App Store Connect** is **connected**. **PostHog** is **disconnected** (`posthog_project_limit`). **Sentry** is **`auth_failed`**. No open alerts.

**Top bottleneck:** `pulse growth review` reports **no urgent bottleneck**. In practice the gap is **instrumentation and billing visibility**: analytics and errors are not reliably flowing, so growth reviews cannot surface concrete acquisition or revenue bottlenecks.

**Recommended next action:** **Fix Sentry authentication**, **unblock or provision PostHog** for this project, then connect **Stripe/QuickBooks, ForgeGraph, entity/compliance, CRM/Growth** so dimensions score and weekly reviews get evidence.
